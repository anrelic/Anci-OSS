buildscript {
    apply from: "dependencies.gradle"
    repositories {
        mavenCentral()
        maven {
            url "https://plugins.gradle.org/m2/"
        }
        maven {
            url "http://dl.bintray.com/jamefrus/maven"
        }
    }

    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:${versions.kotlin}",
                'org.junit.platform:junit-platform-gradle-plugin:1.0.0-M1',
                "su.jfdev.gradle.struct:gradle-struct:1.3.3"
    }
}

plugins {
    id "com.jfrog.bintray" version "1.7"
}

group 'su.jfdev.anrelic'
version '0.1.0'

subprojects {
    apply plugin: 'su.jfdev.struct'
    apply plugin: 'java'
    apply plugin: 'kotlin'
    apply plugin: 'org.junit.platform.gradle.plugin'
    apply plugin: 'maven-publish'

    repositories {
        jcenter()
    }

    dependencies {
        apiCompile lib.kotlin["runtime"]
        compile lib.kotlin["std"]

        testCompile lib.jupiter["api"], lib.junit["platform"]
        testRuntime lib.jupiter["engine"]
    }

    junitPlatform {
        engines {
            include 'junit-jupiter'
        }
    }

    bintray {
        user = property("binuser")
        key = property("binkey")
        configurations = ['general']
        pkg {
            repo = 'maven'
            name = "anci-$name"
            userOrg = property("binuser")
            licenses = ['MIT']
            vcsUrl = 'https://github.com/Jamefrus/OpenAnci.git'
            version {
                name = project.version
                released = new Date()
                vcsTag = project.version
            }
        }
    }
}